---
layout: ../../components/site/BaseLayout.astro
title: Component Preview | learn
description: Internal preview page for the MDX content components.
---

import ContentArticle from "../../components/site/ContentArticle.astro";
import PageShell from "../../components/site/PageShell.astro";
import Note from "../../components/content/Note.astro";
import Tip from "../../components/content/Tip.astro";
import Warning from "../../components/content/Warning.astro";
import Steps from "../../components/content/Steps.astro";
import CascadeInspector from "../../components/content/CascadeInspector.astro";
import BoxModelExplorer from "../../components/content/BoxModelExplorer.astro";
import LayoutLab from "../../components/content/LayoutLab.astro";
import ResponsiveLab from "../../components/content/ResponsiveLab.astro";
import MotionLab from "../../components/content/MotionLab.astro";
import ColorLab from "../../components/content/ColorLab.astro";

<PageShell size="reading" class="py-10 sm:py-12">
  <ContentArticle>
    <h1>Component preview</h1>
    <p>This page is intentionally unlinked. Use it to inspect the MDX content components directly.</p>

    <h2>Note</h2>
    <Note title="npx and npm exec">
      <p><code>npm exec</code> is the underlying modern npm command. <code>npx</code> is still common in tutorials, READMEs, and everyday usage, so it is worth recognizing both forms.</p>
    </Note>

    <h2>Tip</h2>
    <Tip title="Install more than one package at a time">
      <p><code>pnpm add react react-dom</code> adds both packages in one command. That is often cleaner when they belong to the same setup step.</p>
    </Tip>

    <h2>Warning</h2>
    <Warning title="Do not mix package managers">
      <p>If a repository uses <code>pnpm-lock.yaml</code>, keep using pnpm for that repository. Mixed lockfiles make the dependency state harder to reason about.</p>
    </Warning>

    <h2>Steps</h2>
    <Steps>
      <li>Install Node.js.</li>
      <li>Open the project directory.</li>
      <li>Run <code>pnpm install</code>.</li>
    </Steps>

    <h2>CSS Labs</h2>

    <CascadeInspector
      property="color"
      previewContent="Save changes"
      rules={[
        { label: "Element rule", selector: "button", value: "oklch(0.52 0.02 255)", specificity: [0, 0, 1], layer: "base", layerRank: 1, sourceOrder: 1 },
        { label: "Class rule", selector: ".primary", value: "oklch(0.5 0.12 245)", specificity: [0, 1, 0], layer: "components", layerRank: 2, sourceOrder: 2 },
        { label: "Utility rule", selector: ".text-success", value: "oklch(0.55 0.13 150)", specificity: [0, 1, 0], layer: "utilities", layerRank: 3, sourceOrder: 3 },
      ]}
    />

    <BoxModelExplorer
      previewContent="Product card"
      initial={{ width: 240, height: 120, padding: 18, border: 2, margin: 16, boxSizing: "border-box" }}
    />

    <LayoutLab
      items={[
        { content: "Overview" },
        { content: "Pricing" },
        { content: "Docs" },
        { content: "Support" },
      ]}
    />

    <ResponsiveLab
      previewContent="Profile summary"
    />

    <MotionLab
      previewContent="Saved message"
    />

    <ColorLab
      sampleText="Checkout summary"
    />
  </ContentArticle>
</PageShell>
